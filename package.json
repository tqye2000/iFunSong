{
  "name": "ifunsong",
  "version": "0.1.0",
  "private": true,
  "description": "Local web app for creating MP4 lyric videos from songs, images, and timed lyrics.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js"
  },
  "dependencies": {
    "ffmpeg-static": "^5.2.0",
    "ffprobe-static": "^3.1.0"
  },
  "engines": {
    "node": ">=20"
  }
}
